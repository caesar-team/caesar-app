apiVersion: v1
kind: Service
metadata:
  name: link
  labels:
    app: link
spec:
  type: ClusterIP
  selector:
    app: link
  ports:
    - name: http
      port: 80
      targetPort: http
# --- No ingress controller? Expose directly instead (edit type above to NodePort):
# type: NodePort
# ports:
#   - name: http
#     port: 80
#     targetPort: http
#     nodePort: 30080
